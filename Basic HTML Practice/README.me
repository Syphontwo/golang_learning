Creates a simple webpage using html and css.

run with `go run welcome.go` and visit `localhost:8080` to see it in a browser

visit using a query url to see the name change i.e.

`localhost:8080?name=syphontwo` will set the name to syphontwo